# To Do's:

- ### order_id increments +2 — It should only increment +1
- ### ICS No. (Manual Input)
- ### Appendix (Could be generated)
- ### Status Indicator (we could use ClassSelector works like a radio button)
  ``` 

  Green, Red, Yellow

  if (status === "pending") {
    functionYellow();
  }

  else if (status === "canceled") {
    functionRed();
  }

  else {
    functionGreen();
  }


  


  ```
 
