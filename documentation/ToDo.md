# To Do's:

### Collapsable Button (Expand Sidebar) Contrast

    remove scale (separate using id)

### Span Arrow --> Image instead of Generic SVG

### PR Form Landscape (display: grid; grid-template-column: 1fr 2fr;)

### nextBtn separate container for more position control

- ### ICS No. (Manual Input)
- ### Appendix (Could be generated)

- ### Status Indicator (we could use ClassSelector works like a radio button)

  ```

  Green, Red, Yellow

  if (status === "pending") {
    functionYellow();
  }

  else if (status === "canceled") {
    functionRed();
  }

  else {
    functionGreen();
  }



  ```

- ### Soft Copy ( default windows popup, allows user to choose what file type to be saved)
- ### 2 Google Forms | 1 Generated PDf (n8n)
- ### CRUD Feature
- ### Search Bar
- ### View Full Page
- ### Archive Page

## [ W.I.P. ]
