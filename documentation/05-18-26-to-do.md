05.14.26 | Implentations

DON'T FORGET THE workflow_status column




## **Major Revision**
-
-  Apple Insipred Concept; Overhaul the overall UI/UX (After all the components are good to go)
- Password Strength Restriction; 1 special character, 1 uppercase, at least 8 characters in total


## **Minor Revision**
- Analyze the view entries, check if there are missing columns and values, View Entries and CSV Export Format Bug


- Property # format; hypen-dash in  405" -" 03

- PR No. Prefilled (detects the current year, month, and auto-increment,
but add a button for the user to be able to edit if the content is not correct)

- Export format columns is sest to â€” (data type appropriation)

{
"prompt_for_github_copilot": [all_current_available models, Claude Haiku 4.5, GPT 4.5, etc.]
"objective":  To achieve all the requirements below without damaging or breaking the whole system's workflow.
"task": Analyze the view entries, check if there are missing columns and values, View Entries and CSV Export Format Bug
"things_to_consider": ["preserve/ enhance currently working logic", "carefully refine and refactor the necessary files while prioritizing the objective"]
"output": ["A fully functional view entries page for tracking activities and storing"]
"recommendation": if you have a better approach for displaying entries in a table format, feel free to do so.

}



{
"objective":  
"task": 
"things_to_consider":
"output":
"recommendation": 

}



==============================================
Base on User Feedback

Before overhaulling the overall UI/UX, 
make a copy for the working workflows
for safety net



===========================================

INTEGRATION STEPS

Step 1: Import the  App Router
In your main app file (e.g., App.jsx or main.jsx):

Step 2: Add Navigation Link
In your navigation/menu component:

import { useNavigate } from 'react-router-dom'

const navigate = useNavigate()
const roleId = parseInt(sessionStorage.getItem('role_id')) || 3

// Show link only for Admin/SuperAdmin
{(roleId === 1 || roleId === 2) && (
  <button onClick={() => navigate('/approval-dashboard')}>
     Approval Dashboard
  </button>
)}

Step 3: Access It
URL: http://localhost:5173/approval-dashboard
Available to: SuperAdmin & Admin only
Blocked for: Employees (with permission denied message)

KEY FEATURES
Feature	Details
Auto-refresh	Updates every 30 seconds
Urgency Alerts	Red badge for 24h+ pending
Rich Details	All item info, creator, office, division
Inline Actions	Approve/disapprove without navigation
Live Feedback	Toast messages for actions
Role Protected	Admin/SuperAdmin only
Responsive	Works on desktop, tablet, mobile

HOW IT WORKS 
Load Dashboard → Fetches all pending PRs from backend
Display List → Shows each PR with all dynamic details
Filter/Sort → User can filter by status or sort by urgency/amount
Click Approve → Sends to /approve_purchase_request.php
Instant Update → List refreshes, PR removed
Disapprove → Entry locked, clear toast message shown


IMPORTANT
Achieve all these without breaking working parts and DON'T ALTER WORKING LOGIC, ENHANCE AND MAKE THE LOGIC BETTER IF YOU SEE A LOOPHOLE.


=========================================

payload performance testing, stress testing, database optimization handler, 



let's do this sequentially

1. List down all the files you need, stop and explain to me what are the contents you need to identify the root cause of the problem.

2. Whilst refactoring, let's do it file by file (only the necessary files) 

3. Latest Database Schema:



IMPORTANT: Achieve all these without breaking working parts and DON'T ALTER WORKING LOGIC, ENHANCE AND MAKE THE LOGIC BETTER IF YOU SEE A LOOPHOLE.





# ======== FIX LATER ==============

View Entries
Enhance Overall UI/UX (FIGMA)






