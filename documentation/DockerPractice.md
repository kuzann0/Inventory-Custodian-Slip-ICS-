# Docker Every Changes Made Practice:
### (To avoid what happened in V6 Version)

## docker compose down - removing currently runnign containers

## docker compose down -v - to avoid data confliction and db refresh

## docker image prune -a  - removes old images 


## docker compose build --no-cache - npm dependencies and PHP extensions are rebuilt cleanly.

## docker compsose up -d - is to start the engines